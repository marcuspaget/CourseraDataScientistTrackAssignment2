source("../../cacheVector.R")
x
mean(x)
cachemean(x)
bigVec <- makeVector(1:1000) 
 biggerVec <- makeVector(1:100000) 
cachemean(bigVec) 
cachemean(bigVec) 
cachemean(biggerVec) 
cachemean(biggerVec) 
q()
source("cachematrix.R")
ls()
 biggerMatrix <- makeCacheMatrix(c((1,2,3),(4,5,6)))
 biggerMatrix <- makeCacheMatrix(matrix(1:9999,nrow=99)
)

cacheSolve(biggerMatrix) 
y<-(matrix(1:9999,nrow=99))
cacheSolve(y*y) 
 biggerMatrix <- makeCacheMatrix(y*y)
cacheSolve(biggerMatrix) 
biggerMatrix
y
head(y)
y*y
 biggerMatrix <- makeCacheMatrix(y*y)
biggerMatrix
c=rbind(c(1, -1/4), c(-1/4, 1)) 
c
solve(c)    
 biggerMatrix <- makeCacheMatrix(c)
cacheSolve(biggerMatrix) 
quit
q()
